[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "NetFrame"
version = "0.1.0"
description = "Multiprocess async network framework"
dependencies = [
]
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src/"]
include = ["netframe"]